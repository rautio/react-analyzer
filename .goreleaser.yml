# GoReleaser configuration for react-analyzer
# https://goreleaser.com
#
# IMPORTANT: This configuration is used for LOCAL DEVELOPMENT ONLY.
#
# Why? react-analyzer uses go-tree-sitter which requires CGO. Cross-compilation
# with CGO is complex and requires platform-specific C compilers.
#
# Release Strategy:
# - LOCAL: Use this config with `goreleaser build --snapshot --single-target`
#          to test builds on your current platform
# - CI/CD:  GitHub Actions builds on NATIVE platforms (macos/ubuntu/windows runners)
#          and manually packages the results. This is the standard pattern for
#          CGO projects without GoReleaser Pro.
#
# To use GoReleaser for CI/CD packaging, you'd need GoReleaser Pro with the
# `builder: prebuilt` feature (~$300-600/year). See docs/NPM.md for details.

version: 2

before:
  hooks:
    # Ensure dependencies are up to date
    - go mod tidy
    # Tests are run separately in CI before building
    # Uncomment for local testing:
    # - go test ./...

builds:
  - id: react-analyzer
    main: ./cmd/react-analyzer
    binary: react-analyzer
    env:
      - CGO_ENABLED=1
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    # Ignore combinations that don't make sense
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w
      - -X main.Version={{.Version}}
    flags:
      - -trimpath

archives:
  - id: default
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - README.md
      - LICENSE
      - docs/**/*

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - Merge pull request
      - Merge branch
  groups:
    - title: "New Features"
      regexp: "^.*feat[(\\w)]*:+.*$"
      order: 0
    - title: "Bug Fixes"
      regexp: "^.*fix[(\\w)]*:+.*$"
      order: 1
    - title: "Performance Improvements"
      regexp: "^.*perf[(\\w)]*:+.*$"
      order: 2
    - title: "Other Changes"
      order: 999

release:
  github:
    owner: rautio
    name: react-analyzer
  draft: false
  prerelease: auto
  name_template: "v{{.Version}}"
  header: |
    ## React Analyzer v{{.Version}}

    Download the appropriate binary for your platform below.

    ### Installation

    **macOS/Linux:**
    ```bash
    # Extract the archive
    tar -xzf react-analyzer_{{.Version}}_<OS>_<ARCH>.tar.gz

    # Move to your PATH
    sudo mv react-analyzer /usr/local/bin/

    # Verify installation
    react-analyzer --version
    ```

    **Windows:**
    ```powershell
    # Extract the zip file
    # Add the directory to your PATH or move react-analyzer.exe to a directory in your PATH

    # Verify installation
    react-analyzer.exe --version
    ```
  footer: |
    ---

    **Full Changelog**: https://github.com/rautio/react-analyzer/compare/{{.PreviousTag}}...{{.Tag}}

# Announce releases (optional - can enable later)
# announce:
#   twitter:
#     enabled: false
#   discord:
#     enabled: false
